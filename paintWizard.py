class Paint():
    def __init__(self):

        p1 = ['CheapoMax', 20, 19.99, 10]
        p2 = ['AverageJoes', 15, 17.99, 11]
        p3 = ['DuluxourousPaints', 10, 25.00, 20]
        self.roomWidth = roomWidth
        self.roomHeight = roomHeight
        self.roomWidth = float(input("Enter the room width, it must be between 1m-25m: "))
        self.roomHeight = float(input("Enter the room height, it must be between 2m-6m: "))

        def calCost(self, roomSize):

            self.roomSize = (roomWidth*roomHeight)*4
            for(p1, p2, p3):
                prices.add(paints.getTotalPrice*(paints.getCoverage/roomSize))
            
            lowestPrice = prices.get(0)
            if (price < lowestPrice):
                lowestPrice = price

            
